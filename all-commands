// replace branch-name with your preferred branch name

// to create a new git branch. 
git branch branch-name

// to list all the branches.
git branch

// to switch between the branches.
git checkout branch-name

// to create and directly switch to created branch.
git checkout -b branch-name

// to delete the branch go to the master branch and type this command.
git branch -d branch-name

// to update the files into github, you can replace the . with filename, the . will update all the files in github with your current commiting branch. change the branch-name where you want to commit the changes to.
git add .
git push origin branch-name
